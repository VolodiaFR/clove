# Copy this file to electron-builder.env and replace the placeholder with the
# permanent public HTTPS root for your own update feed. Official releases should
# use an R2 custom domain, not an r2.dev development URL. This URL is public
# runtime configuration; credentials and signing material never belong here.
CLOVE_UPDATE_URL=https://updates.example.com/clove
